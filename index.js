/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Login from './components/login/login_screen.js';

AppRegistry.registerComponent(appName, () => Login);
